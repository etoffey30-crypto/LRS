
  # Revise Login Signup Structure

  This is a code bundle for Revise Login Signup Structure. The original project is available at https://www.figma.com/design/r0XfDLztePHVj3ToaIsbR5/Revise-Login-Signup-Structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  